# Python reference implementation

Implements deterministic canonicalization, domain-separated hashing, PC digesting, verification, fallback decisions, and vector-based conformance tests.

## Run

```bash
python -m pip install -e .[dev]
pytest -q
```
