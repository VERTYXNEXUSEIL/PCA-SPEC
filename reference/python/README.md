# PCA Python reference implementation

Implements deterministic canonical encoding, domain-separated hashing, Plan Certificate construction, verification, execution fallback, and vector harness.

## Run tests

```bash
python -m pytest -q
```

## CLI

```bash
python ../cli/pca_cli.py run-vectors ../../test-vectors/vectors-v1
```
