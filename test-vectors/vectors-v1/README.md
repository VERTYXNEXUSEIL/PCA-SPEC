# vectors-v1

Run with `python reference/cli/pca_cli.py run-vectors test-vectors/vectors-v1`.
Each vector contains `inputs`, `pc`, `runtime`, and `expected`.

## Note on Test Coverage

T7 (Optional-field canonical omission) test vector is not yet implemented. Bronze conformance (T1-T6, T8) excludes T7 pending completion of this test vector.
